<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="fernandoperez.lifemanager.activities.SettingsActivity"
    tools:showIn="@layout/activity_settings">

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/content_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">
        <!--tools:context="fernandoperez.lifemanager.activities.SettingsActivity"-->
        <!--tools:showIn="@layout/activity_settings">-->

        <ImageView
            android:id="@+id/settings_logo_imageview"
            android:layout_width="match_parent"
            android:layout_height="160dp"
            android:src="@drawable/polimi_logo"
            />

        <Button
            android:id="@+id/settings_backup_button"
            android:layout_below="@id/settings_logo_imageview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/settings_backup"
            android:onClick="askForBackup"/>

        <Button
            android:id="@+id/settings_aboutus_button"
            android:layout_below="@id/settings_backup_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/settings_aboutus"
            android:onClick="startAboutUs"/>

    </RelativeLayout>
</android.support.v4.widget.NestedScrollView>